services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: leadflux
      POSTGRES_PASSWORD: leadflux
      POSTGRES_DB: leadflux
    ports:
      - "5433:5432"
    volumes:
      - leadflux-postgres-data:/var/lib/postgresql/data

volumes:
  leadflux-postgres-data:
